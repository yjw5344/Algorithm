#include <string>
#include <vector>

using namespace std;

int solution(int n, vector<int> lost, vector<int> reserve) {
  vector<int> result;
  result.resize(n+1);
  for( auto i : result){
    i =
  }
  for(int i = 1 ; i < reserve.size() ; i++){
    reserve.at(i) = 2;
  }



  int answer = 0;
  return answer;
}
