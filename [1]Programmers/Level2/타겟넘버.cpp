#include <iostream>
#include <string>
#include <vector>
using namespace std;

int answer = 0;

void dfs(vector<int> numbers, int target, int k ){
  int len = numbers.size();




}


int solution(vector<int> numbers, int target) {
  dfs(numbers, target, 0);


    return answer;
}

int main(){
  vector<int> arr;
  int target = 3;

  arr.push_back(1);
  arr.push_back(1);
  arr.push_back(1);
  arr.push_back(1);
  arr.push_back(1);

  cout << solution(arr, target) << endl;

  return 0;
}
