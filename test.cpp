#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main(){

  vector<int> arr;
  arr.push_back(10);
  arr.push_back(15);
  arr.push_back(12);
  arr.push_back(111);
  arr.push_back(17);
  arr.push_back(156);
  //
  // int max = *max_element(arr.begin(), arr.end());
  // cout << max << endl;

  // int num = *max_element(arr.at(0), arr.at(1), arr.at(2));
  // cout << max << endl;



  return 0;
}
